# Task Tracker: Invisible Interactive UI (V6)

- [ ] UI Cleanup (`app.js`)
    - [ ] Remove `hotspot-text` injection in `renderHotspots`
- [ ] CSS Refinement (`style.css`)
    - [ ] Hide `.hotspot-box` by default (border/bg)
    - [ ] Add very subtle hint on `:hover`
    - [ ] Keep `.reading` spotlight clear
- [ ] Manual Verification
    - [ ] Verify clean page look
    - [ ] Test click activation
- [ ] Commit & Push to Git
- [ ] Create `walkthrough_v6.md`
